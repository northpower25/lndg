name: CI

on:
  pull_request:
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  test-backend:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt ruff

      - name: Prepare minimal test environment
        run: |
          mkdir -p /tmp/lnd
          touch /tmp/lnd/tls.cert /tmp/lnd/admin.macaroon /tmp/lnd/channel.db
          python initialize.py --tlscert /tmp/lnd/tls.cert --macaroon /tmp/lnd/admin.macaroon --lnddatabase /tmp/lnd/channel.db --force

      - name: Run lint
        run: make lint

      - name: Run migration check
        run: make check

      - name: Run tests
        run: make test

  test-frontend:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      - name: Cache Node dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Check for frontend package.json
        id: check_pkg
        run: |
          if [ -f package.json ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install and test frontend
        if: steps.check_pkg.outputs.exists == 'true'
        run: |
          npm ci
          npm test --if-present
