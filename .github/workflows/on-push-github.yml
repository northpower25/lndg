name: Build on release

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/docker-publish.yml
    with:
      tag: ${{ github.event_name == 'workflow_dispatch' && 'latest' || github.event.release.tag_name }}
    secrets: inherit
