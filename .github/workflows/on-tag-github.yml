name: Build on tag

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to build (e.g. v1.11.2)'
        required: true

jobs:
  build:
    uses: ./.github/workflows/docker-publish.yml
    with:
      tag: ${{ github.event.inputs.tag }}
    secrets: inherit
